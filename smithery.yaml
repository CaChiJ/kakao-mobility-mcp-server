app:
  name: kakao-mobility-mcp-server
  version: 1.0.0

build:
  type: docker
  dockerfile: ./Dockerfile

deploy:
  env:
    - name: KAKAO_REST_API_KEY
      required: true

  port: 3000
  
  health:
    path: /health
    port: 3000

  resources:
    memory: 512
    cpu: 1 